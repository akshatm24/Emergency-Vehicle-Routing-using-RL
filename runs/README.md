# Run Artifacts

This directory contains previously executed notebook runs, audit reports, cache files, and generated visualizations from development.

The most presentation-ready artifacts have been copied to:

```text
artifacts/final_run/
```

Use this folder if you want the detailed execution trail. Use `artifacts/final_run/` and `notebooks/kanpur_emergency_routing_final.ipynb` for grading or review.
